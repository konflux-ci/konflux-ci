apiVersion: v1
kind: Service
metadata:
  name: {{ include "konflux-ci.fullname" . }}
  labels:
    {{- include "konflux-ci.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "konflux-ci.selectorLabels" . | nindent 4 }}
