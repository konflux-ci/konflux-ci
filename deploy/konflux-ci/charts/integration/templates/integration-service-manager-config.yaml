apiVersion: v1
kind: ConfigMap
metadata:
  name: integration-service-manager-config
  namespace: {{ .Values.IntegrationServiceNamespace }}
  labels:
  {{- include "integration.labels" . | nindent 4 }}
data:
  controller_manager_config.yaml: {{ .Values.integrationServiceManagerConfig.controllerManagerConfigYaml
    | toYaml | indent 1 }}
