apiVersion: v1
kind: ServiceAccount
metadata:
  name: integration-service-controller-manager
  namespace: integration-service
  labels:
  {{- include "integration.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.integrationServiceControllerManager.serviceAccount.annotations | nindent 4 }}
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: integration-service-snapshot-garbage-collector
  namespace: integration-service
  labels:
  {{- include "integration.labels" . | nindent 4 }}
  annotations:
    {{- toYaml .Values.integrationServiceSnapshotGarbageCollector.serviceAccount.annotations | nindent 4 }}
