# This kustomization.yaml is not intended to be run by itself,
# since it depends on service name and namespace that are out of this kustomize package.
# It should be run by config/default
resources:
- bases/konflux.konflux-ci.dev_konfluxes.yaml
- bases/konflux.konflux-ci.dev_konfluxbuildservices.yaml
- bases/konflux.konflux-ci.dev_konfluxintegrationservices.yaml
- bases/konflux.konflux-ci.dev_konfluxreleaseservices.yaml
- bases/konflux.konflux-ci.dev_konfluxuis.yaml
- bases/konflux.konflux-ci.dev_konfluxrbacs.yaml
- bases/konflux.konflux-ci.dev_konfluxnamespacelisters.yaml
- bases/konflux.konflux-ci.dev_konfluxenterprisecontracts.yaml
- bases/konflux.konflux-ci.dev_konfluximagecontrollers.yaml
- bases/konflux.konflux-ci.dev_konfluxapplicationapis.yaml
# +kubebuilder:scaffold:crdkustomizeresource

patches:
# [WEBHOOK] To enable webhook, uncomment all the sections with [WEBHOOK] prefix.
# patches here are for enabling the conversion webhook for each CRD
# +kubebuilder:scaffold:crdkustomizewebhookpatch

# [WEBHOOK] To enable webhook, uncomment the following section
# the following config is for teaching kustomize how to do kustomization for CRDs.
#configurations:
#- kustomizeconfig.yaml
