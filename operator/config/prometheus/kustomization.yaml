# Operator ServiceMonitor and scraper ClusterRoleBinding are reconciled at runtime by
# internal/operatormetrics.ScrapeTokenRotator (not installed via GitOps).
# TLS config (metrics-server-cert ca.crt + serverName) lives in scrape_wiring.go — there is no
# kustomize ServiceMonitor patch for the operator.
resources: []
