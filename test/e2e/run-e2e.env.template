# Configuration for the E2E test suite.
# Copy this file to run-e2e.env in the same directory and fill in the values.
#
# Alternatively, these variables can be exported into your shell environment.
# If a variable is set in the environment, it will take precedence over the value here.

# Your Quay.io organization where test images will be pushed.
QUAY_ORG=""

# Your GitHub organization or username where the test repository fork is located.
GH_ORG=""

# A GitHub Personal Access Token with permissions to merge PRs in your GH_ORG.
GH_TOKEN=""

# The content of your Quay.io credentials (docker config.json) for a robot account
# with write access to the QUAY_ORG. This should be the raw JSON content.
# Example: QUAY_DOCKERCONFIGJSON='{"auths": ...}'
QUAY_DOCKERCONFIGJSON=""

# The full name of the E2E test image to be used.
# Default: "quay.io/konflux-ci/e2e-tests:latest"
E2E_TEST_IMAGE="quay.io/konflux-ci/e2e-tests:latest"
